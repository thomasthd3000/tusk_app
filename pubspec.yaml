name: course_tusk_app
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.2.6 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  d_button: ^0.0.10
  d_info: ^0.2.0
  d_input: ^0.1.3
  d_method: ^0.2.6
  d_session: ^0.0.2
  extended_image: ^8.2.0
  flutter_bloc: ^8.1.3
  gap: ^3.0.1
  get: ^4.6.6
  google_fonts: ^6.1.0
  http: ^1.2.0
  image_picker: ^1.0.7
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
