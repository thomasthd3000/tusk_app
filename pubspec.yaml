name: tusk_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.4 <4.0.0'

dependencies:
  d_button: ^0.0.10
  d_info: ^0.2.0
  d_input: ^0.1.3
  d_method: ^0.2.6
  d_session: ^0.0.3
  extended_image: ^8.3.0
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.6
  gap: ^3.0.1
  get: ^4.6.6
  google_fonts: ^6.1.0
  http: ^1.2.2
  image_picker: ^1.1.2
  intl: ^0.19.0

dev_dependencies:
  flutter_lints: ^3.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/
